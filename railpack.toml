# Railpack configuration for Vercel serverless deployment
# Vercel handles deployment automatically, so no deploy command is needed

[build]
# Build step just installs dependencies - no additional build needed
build = "npm ci"

[deploy]
# Skip deploy - Vercel handles this automatically
deploy = "echo 'Skipping deploy - Vercel handles serverless function deployment'"

